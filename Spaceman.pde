Player player;
Pillars[] pillars = new Pillars[2]; // array for pillars
PImage bgImage;
void setup()
{
  size (500, 800);
  bgImage = loadImage("background.png");
}

void draw()
{
  
  background(bgImage);
}